{{> nav}}
<div class='lg:flex'>
  {{> filters}}
  <main class='scroll my-3 col-span-4 line'>
    <div id='itemCards' class='flex flex-wrap gap-4 m-8 ml-14'>
      {{#each items as |item|}}
        {{> browseCard}}
      {{/each}}
    </div>
  </main>
</div>
