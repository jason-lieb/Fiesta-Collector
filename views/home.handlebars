{{! Collection page }}

{{> nav}}
{{> filters}}

{{! each loop of itemCards }}
<h1>From home.handlebars file</h1>
{{#each serializedData as |data|}}
  {{!-- {{> itemCard}} --}}
  <p>{{data.item_name}}</p>
{{/each}}
