{{! Card with picture and item name, color, quantity and delete buttons }}
<div style="border: 1px black solid;">
  {{!-- Delete button if on home page --}}
  {{!-- Item.pictureLocation? --}}
  <p>{{item_name}}</p>
  {{!-- color if on home page --}}
  {{!-- quantity if on home page --}}
</div>